# Pulumi OPNsense Provider for Node.js

This directory contains generated Node.js SDK artifacts used during packaging.

For primary usage and examples, see:

- `sdk/nodejs/README.md`

The SDK is generated from the provider schema via:

```bash
make codegen
```
